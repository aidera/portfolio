import IconedButton from '../_ui/IconedButton/IconedButton';
import CircleIcon from '../../assets/icons/CircleIcon';

export default function BackToMenuButton() {
  return <IconedButton text="Back to Menu" link='/' icon={<CircleIcon />} />;
}
